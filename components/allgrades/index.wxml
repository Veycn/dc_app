<!--components/allgrades/index.wxml-->
<view class="all-wrap">
	<block wx:for="{{ allGrades }}" wx:key="index">
		<view bindtap="chooseGrade" data-idx="{{ item.id }}" data-index="{{ index }}" class="all-item {{ choosed === index ? 'active' : '' }}">
			{{ item.grade }}
		</view>
	</block>
</view>

